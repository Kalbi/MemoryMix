enum tracknum { 
	none,
	track1
}

enum controltype {
	keyboard,
	xbox
}

enum actions {
	both,
	keys,
	swipe
}


curr_track = tracknum.track1;

curr_controltype = controltype.keyboard;

curr_actions = actions.both;
